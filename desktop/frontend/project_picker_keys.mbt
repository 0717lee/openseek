// The project picker's keyboard and clipboard surface moved into the
// `project_picker` package (`project_picker/keys.mbt`), registered there
// through `@project_picker.subscriptions` in `boot.mbt`. This file no
// longer holds definitions.
